<frozen runpy>:128: RuntimeWarning: 'os_agent.agents.master' found in sys.modules after import of package 'os_agent.agents', but prior to execution of 'os_agent.agents.master'; this may result in unpredictable behaviour
======================================================================
TEST 1: Keyword classifier only (no model)
======================================================================
  OK     files       (files, score=2)  <- Find all files larger than 100MB on Linux
  OK     files       (files, score=2)  <- Find files modified in the last 24 hours in /var/log
  OK     files       (files, score=1)  <- Recursively search for the string 'ERROR' in all .log f
  OK     files       (files, score=1)  <- What does chmod 755 do and when would you use it?
  OK     files       (files, score=2)  <- How do I change the owner of a directory and all its co
  OK     files       (files, score=2)  <- How do I create a symbolic link?
  OK     files       (files, score=3)  <- How do I extract the 3rd column from a space-separated 
  OK     files       (files, score=2)  <- How do I replace all occurrences of 'foo' with 'bar' in
  OK     files       (files, score=4)  <- How do I count lines, words, and characters in a file?
  OK     files       (files, score=4)  <- How do I sort a file and remove duplicate lines?
  OK     files       (files, score=2)  <- How do I mount a USB drive on Linux?
  MODEL  files       scores={'files': 1, 'process': 1}  <- How do I check available disk space on all mounted file
  OK     files       (files, score=4)  <- How do I create a compressed tar.gz archive of a direct
  OK     files       (files, score=2)  <- How do I find and delete files older than 30 days?
  OK     files       (files, score=3)  <- Write a bash script that checks if a file exists and pr
  OK     files       (files, score=4)  <- How do I loop over all .log files in a directory in bas
  OK     files       (files, score=2)  <- How do I capture the output of a command into a variabl
  OK     files       (files, score=3)  <- How do I pass arguments to a bash script and validate t
  OK     network     (network, score=2)  <- List all open TCP ports on the system
  OK     network     (network, score=1)  <- Generate an SSH key pair and add it to authorized_keys
  OK     network     (network, score=3)  <- How do I copy a file to a remote server using SCP?
  OK     network     (network, score=1)  <- How do I check my current IP address on Linux?
  OK     network     (network, score=3)  <- How do I test if a remote port is open without telnet?
  OK     network     (network, score=2)  <- How do I block port 22 with iptables?
  OK     network     (network, score=3)  <- How do I use rsync to sync a local folder to a remote s
  OK     process     (process, score=2)  <- How do I check disk usage broken down by directory?
  OK     process     (process, score=3)  <- How do I kill a process by name without knowing its PID
  OK     process     (process, score=2)  <- Show me how to find which process is using the most mem
  OK     process     (process, score=2)  <- How do I run a process in the background and keep it af
  OK     process     (process, score=3)  <- How do I schedule a cron job to run a script every day 
  OK     process     (process, score=3)  <- How do I check CPU and memory usage in real time?
  OK     process     (process, score=3)  <- How do I add a user to the sudo group?
  MODEL  process     scores={'files': 1, 'process': 1}  <- How do I create a new user with a home directory?
  OK     process     (process, score=3)  <- How do I lock a user account without deleting it?
  OK     process     (process, score=2)  <- How do I view all groups a user belongs to?
  OK     process     (process, score=1)  <- How do I start, stop, and restart a systemd service?
  OK     process     (process, score=1)  <- How do I check if a service is enabled on boot with sys
  OK     packages    (packages, score=3)  <- How do I install a .deb package manually?
  OK     packages    (packages, score=4)  <- How do I find which package owns a specific file on Deb
  OK     kernel      (kernel, score=2)  <- What is a Linux kernel module and how do you load one?
  MODEL  kernel      scores={'process': 1, 'kernel': 1}  <- Explain the difference between a process and a thread i
  OK     kernel      (kernel, score=2)  <- How does virtual memory paging work in Linux?
  MODEL  kernel      scores={'files': 1, 'kernel': 1}  <- What is the purpose of the /proc filesystem?
  OK     kernel      (kernel, score=1)  <- How do I check the current kernel version and build inf

Keyword resolved: 40/44 (90.9%)
Needs model fallback: 4/44

Questions needing model:
  [files] How do I check available disk space on all mounted filesystems?  scores={'files': 1, 'process': 1}
  [process] How do I create a new user with a home directory?  scores={'files': 1, 'process': 1}
  [kernel] Explain the difference between a process and a thread in Linux  scores={'process': 1, 'kernel': 1}
  [kernel] What is the purpose of the /proc filesystem?  scores={'files': 1, 'kernel': 1}
