# Ollama Modelfile for Qwen 3.5 4B OS Assistant (Q4_K_M)
#
# Import:
#   ollama create os-ai -f Modelfile
#
# Run:
#   ollama run os-ai "list all running services"

FROM ./qwen3.5-4b-os-q4km.gguf

# Context window — 512 is plenty for OS commands, saves ~60% KV cache memory
PARAMETER num_ctx 512

# Low temperature for deterministic shell commands
PARAMETER temperature 0.1

# Slight top_p to allow some variation in explanations
PARAMETER top_p 0.9

# Cap response length — action answers should be short
PARAMETER num_predict 150

# Stop tokens — prevent rambling
PARAMETER stop "<|im_end|>"
PARAMETER stop "<|endoftext|>"
PARAMETER stop "</think>"

# Disable thinking — prefill empty think block so model skips reasoning
TEMPLATE """{{- if .System }}<|im_start|>system
{{ .System }}<|im_end|>
{{ end }}<|im_start|>user
{{ .Prompt }}<|im_end|>
<|im_start|>assistant
<think>

</think>

"""

SYSTEM """You are an AI assistant built into a Linux-based operating system. Respond with one correct command in a bash code block followed by a one-line explanation. For conceptual questions, explain in 2-4 sentences. If the request is ambiguous, ask one clarifying question. Never list alternatives. Never restate the question. Never explain individual flags."""
